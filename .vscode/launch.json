{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Datawarehouse",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd"
        }
    ]
}